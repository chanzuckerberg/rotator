language: go
go:
  - "1.12"
os:
  - linux
env:
  - GO111MODULE=on
install:
  - make setup
jobs:
  include:
    - stage: test
      script:
        - make test-coverage
      after_success:
        - bash <(curl -s https://codecov.io/bash)
    - stage: test
      script:
        - make lint
